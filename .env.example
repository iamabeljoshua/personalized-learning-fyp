# Root .env — used by docker-compose for postgres variable substitution
POSTGRES_USER=tutor
POSTGRES_PASSWORD=pass
POSTGRES_DB=agentic_tutor

# See also:
#   apps/backend/.env          — local dev config (DB_HOST=localhost, etc.)
#   .env.docker                — Docker container config (DB_HOST=postgres, etc.)
